# Server-only Google Maps key (Geocoding + Places API New). Primary business source.
# Enable Geocoding API and Places API (New) on the key in Google Cloud Console.
GOOGLE_MAPS_API_KEY=
# Set to false only to force RapidAPI/OpenStreetMap instead of Google.
ENABLE_GOOGLE_PLACES=true

# Optional server-only OpenAI-compatible endpoint for profile / outreach drafting.
RESEARCH_API_KEY=
RESEARCH_MODEL=
RESEARCH_BASE_URL=

# Fallback geocoding contact for Nominatim when RapidAPI/Google are unavailable.
OSM_CONTACT_EMAIL=
OVERPASS_API_URL=https://overpass-api.de/api/interpreter

# RapidAPI Maps Data — primary Google Maps business listings provider.
# https://rapidapi.com/letscrape-6bRBa3QguO5/api/maps-data
RAPIDAPI_KEY=
RAPIDAPI_HOST=maps-data.p.rapidapi.com

# Official FCC fixed-availability index. Download FCC state CSVs through your FCC
# account, unzip them, then run `npm run fcc:import -- ...` as documented in README.
FCC_USERNAME=
FCC_HASH_VALUE=
FCC_AVAILABILITY_DB_PATH=

# Optional commercial Fabric address matching for exact FCC Location IDs.
# Without this, ProspectIQ can only show official H3-area observations.
COSTQUEST_API_TOKEN=
COSTQUEST_MATCH_MIN_SIMILARITY=0.95

# Explicit opt-in only. The normal default is live OpenStreetMap data.
USE_DEMO_DATA=false

# Public GitHub link shown on the landing header.
NEXT_PUBLIC_GITHUB_URL=https://github.com/sforskeezy/findbiz

# Optional database connection for later phases.
DATABASE_URL=
